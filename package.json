{
  "name": "fullstack-application",
  "version": "1.0.0",
  "description": "A production-quality inventory management system with a search-focused UI and a database-backed backend.",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install sqlite3 --build-from-source && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "start": "cd backend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
